# テストデータ作成の運用メモ

## 基本方針
- テストデータは、小規模なら原則1ファイルで管理する。
- 複雑な場合は `insert / verify / cleanup` に分けてもよい。
- 正常系と「検知されない（対象外になる）ケース」を必ずセットで用意する。
- 後で特定・削除できるよう、テストデータと分かる識別子を値に含める。

## 作成手順
1. 先にプロダクトのSQLを書き、期待結果（件数・対象ID）を固定する。
2. テーブルごとに必要最小限の列だけ投入する。
   - ただし、JOINキー・期間条件列・ステータス列など判定に効く列は省略しない。
3. 正常系を先に作り、まず1件で通す。
4. 対象外ケース（期間外・条件不一致など）を追加する。
5. 件数だけでなく、対象レコードの中身も確認する。
6. 再実行できるように `cleanup` を入れる。もしくは、同じSQLを複数回実行しても問題ない構成にする。

## 変数化ルール

### 変数化する（変更頻度が高い）
- 対象期間（`@start_at`, `@end_at`）
- 条件キー（`@tracker_code` など）
- テストIDのベース（`@id_base`）

### 変数化しない（固定のほうが読みやすい）
- ケース識別子としての値（`CASE_A`, `USER_001` など）
- 変更されにくい説明的な固定値