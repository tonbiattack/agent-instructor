# 実務テーブル設計パターン集

## 目的
実務で頻出するテーブル設計の型を整理し、要件に応じて使い分けるための判断材料を提供する。

## 共通ルール
- 原則、全テーブルに `created_at` と `update_at` を持たせる
- 履歴系テーブル（イベント・監査ログ）は `update_at` を持たせない
- `status` は `CHECK` 制約ではなくステータスマスタで管理する

## パターン1: Mutable（現在値のみ）
用途:
- プロフィール、設定、メタ情報

特徴:
- 1エンティティ1行を UPDATE で保守
- 読み取りは高速でシンプル
- 履歴追跡は弱い

向いている領域:
- 一般的な業務アプリの基本情報管理

## パターン2: Immutable Event（イベント蓄積のみ）
用途:
- 監査、金融、会計、ワークフロー、状態遷移

特徴:
- UPDATE せず INSERT のみ
- 変更履歴を完全保存できる
- 現在値参照に集約クエリが必要で重くなりやすい

向いている領域:
- 改ざん耐性・説明責任が強く求められる領域

## パターン3: Hybrid（実務で最頻出）
構成:
- `*_events`（履歴）
- `*`（現在値）

更新フロー:
1. 履歴テーブルへ INSERT
2. 現在値テーブルを UPDATE

特徴:
- 履歴保持と高速参照を両立
- 運用・開発のバランスがよい

向いている領域:
- 状態遷移が多く、かつ一覧・参照性能も必要なシステム

## パターン4: Status Master + FK
構成:
- `status_master`（`status_id`, `status_code`, `status_name`, `sort_order`, `is_active`）
- 業務テーブルは `status_id` を外部キー参照

特徴:
- ステータス定義を一元管理できる
- 表示順や有効/無効管理に強い
- `CHECK` より変更容易で運用向き

## 適用判断
- 監査・不正検知・金融取引: Hybrid か Immutable Event を優先
- マスタ属性・プロフィール: Mutable を優先
- 状態の選択肢が増減する: Status Master + FK を必須化

## 最小セット推奨
- `users`（現在値）: `created_at`, `update_at` を保持
- `user_status_events`（履歴）: `created_at` のみ保持（`update_at` なし）
- `status_master`（ステータスマスタ）: 定義を集中管理

## 補足
- イミュータブル化は全テーブル一律ではなく、イベント系に限定する
- 参照性能が課題なら、現在値テーブル・集約テーブル・インデックスで補う
