# テストファースト開発ガイド

## 基本原則
実装コードを書く前に、必ずテストコードを先に作成する。

## テスト手法の方針（古典派）
- **データベースアクセスは実DBを使用**（モックを使用しない）
- **モックは外部API等の外部結合のみ**に限定
- 内部で閉じている実装に対してモックを使用しない
- 実際の動作を確認することでテストの信頼性を向上

## 作業手順

### 1. 要件を理解する
- ユーザーストーリーや仕様を確認
- 期待される動作を明確にする

### 2. テストケースを設計する
- 正常系、異常系、境界値をリストアップ
- テストすべき項目を洗い出す

### 3. テストコードを作成する
- 実装前に失敗するテストを書く
- テストケース名で何をテストするか明示する
- 各テストは独立して実行可能にする

### 4. インターフェースを定義する
- テストから必要な関数・クラスのシグネチャを決める
- 実装すべきAPIを明確にする

## ルール
- テストは実装前に必ず作成
- テストコードは読みやすく保守しやすい構造
- **データベースアクセスは実DBを使用（モック禁止）**
- **モックは外部API等の外部結合のみに限定**
- 一つの実装に複数のテストファイルがあってもOK
  - 単体テスト: `user.test.ts`
  - 統合テスト: `user.integration.test.ts`
  - E2Eテスト: `user.e2e.test.ts`

## ディレクトリ構成例
```
tests/
├── unit/          # 単体テスト
├── integration/   # 統合テスト
└── e2e/          # E2Eテスト
```

## チェックリスト
- [ ] 要件を理解した
- [ ] テストケースを列挙した
- [ ] テストコードを作成した（実装前）
- [ ] インターフェースを定義した
- [ ] テストが要件を網羅している
