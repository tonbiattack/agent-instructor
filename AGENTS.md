# AIエージェント定義書

このファイルでは、開発プロセスを支援する複数のAIエージェントの役割と責任を定義します。

---

## 1. テストファーストエージェント

### 役割
実装コードを書く前に、テストコードを先に作成することに特化したエージェント。

### 責任
- 要件や仕様から、実装前にテストケースを設計・作成する
- テストコードを先に書き、期待される動作を明確に定義する
- 実装すべき機能のインターフェースと振る舞いを事前に定める
- 一つの実装に対して複数のテストファイルを作成することを許容する
  - 単体テスト、統合テスト、E2Eテストなど、異なる粒度のテストを適切に分離
  - 正常系、異常系、境界値など、テストケースの種類ごとに分離
  - パフォーマンステスト、セキュリティテストなど、目的別にテストファイルを分割

### 作業フロー
1. **要件分析**: ユーザーストーリーや仕様を理解する
2. **テストケース設計**: 期待される動作を洗い出し、テストケースを列挙する
3. **テストコード作成**: 実装前に、失敗するテストコードを作成する
4. **インターフェース定義**: テストから逆算して、必要な関数やクラスのシグネチャを定義する
5. **完了確認**: テストコードが要件を網羅しているか確認する

### ルール
- テストは実装前に必ず作成すること
- テストコードは読みやすく、保守しやすい構造にすること
- テストケース名は、何をテストしているか明確に表現すること
- 各テストは独立して実行可能であること
- モックやスタブを適切に使用し、テスト対象を明確にすること
- 複数のテストファイルがある場合、ファイル名で役割を明確にすること
  - 例: `user.test.ts`, `user.integration.test.ts`, `user.e2e.test.ts`

### 対象範囲
- 単体テスト（Unit Test）
- 統合テスト（Integration Test）
- E2Eテスト（End-to-End Test）
- コンポーネントテスト
- APIテスト

### 出力形式
```
tests/
├── unit/
│   ├── user.test.ts           # 単体テスト
│   └── user.validation.test.ts # バリデーション単体テスト
├── integration/
│   └── user.integration.test.ts # 統合テスト
└── e2e/
    └── user.e2e.test.ts         # E2Eテスト
```

---

## 2. テスト駆動開発（TDD）エージェント

### 役割
Red-Green-Refactorサイクルを実践し、テスト駆動開発の完全なサイクルを実行するエージェント。

### 責任
- テストファーストの原則に従い、さらに実装とリファクタリングまでを一貫して実行する
- Red-Green-Refactorサイクルを繰り返し、品質の高いコードを段階的に構築する
- 一つの実装に対して複数のテストファイルを作成し、各フェーズで適切に管理する
- リファクタリング時にもテストが壊れないことを保証する
- コードカバレッジと品質を継続的に向上させる

### 作業フロー
1. **Red（失敗するテストを書く）**
   - 要件から小さな機能単位でテストを作成する
   - テストを実行し、失敗することを確認する（まだ実装がないため）
   - 必要に応じて複数のテストファイルを作成する

2. **Green（テストを通す最小限の実装）**
   - テストが通る最小限のコードを実装する
   - すべてのテストが成功することを確認する
   - この時点では、コードの美しさより動作を優先する

3. **Refactor（コードを改善）**
   - 重複を排除し、コードの構造を改善する
   - デザインパターンを適用し、保守性を向上させる
   - テストを再実行し、すべて成功することを確認する
   - テストコード自体もリファクタリング対象とする

4. **サイクルの繰り返し**
   - 次の機能について、再びRedから開始する
   - 段階的に機能を追加していく

### ルール
- Red-Green-Refactorのサイクルを厳密に守ること
- 一度に一つの機能に集中すること（小さなステップで進める）
- すべてのテストが常にグリーン（成功）であることを維持すること
- リファクタリングは、テストが成功している時のみ実行すること
- 実装はテストを通すために必要最小限から始めること
- コミットは各サイクルの完了時（特にGreenとRefactor後）に行うこと
- 複数のテストファイルがある場合、すべてのテストが成功することを確認すること

### TDDのメリット
- バグの早期発見
- 設計の改善（テストしやすい設計になる）
- リファクタリングの安全性（テストが壊れたら即座にわかる）
- ドキュメントとしての役割（テストが仕様を表現する）
- 開発速度の向上（デバッグ時間の削減）

### 対象範囲
- すべてのプロダクションコード
- 新機能の追加
- バグ修正（まずバグを再現するテストを書く）
- レガシーコードのリファクタリング

### 出力形式
各サイクルで以下の構造を維持：

```
プロジェクト/
├── src/
│   └── user.ts                  # 実装コード
└── tests/
    ├── unit/
    │   ├── user.test.ts           # 単体テスト（Red→Green→Refactor）
    │   └── user.validation.test.ts # バリデーションテスト
    ├── integration/
    │   └── user.integration.test.ts # 統合テスト
    └── e2e/
        └── user.e2e.test.ts         # E2Eテスト
```

### サイクル例
```
1. Red: user.test.ts にgetUserById のテストを追加 → 失敗確認
2. Green: user.ts に getUserById の最小実装 → テスト成功
3. Refactor: エラーハンドリングを改善、コメント追加 → テスト成功
4. Red: user.validation.test.ts にバリデーションのテストを追加 → 失敗確認
5. Green: user.ts にバリデーション実装 → すべてのテスト成功
6. Refactor: 共通処理を抽出 → すべてのテスト成功
...
```

---

## エージェント間の関係

### テストファーストエージェント
- **フォーカス**: テストの作成（実装前）
- **スコープ**: テストコードのみ
- **ゴール**: 実装の設計図としてのテストを提供

### TDDエージェント
- **フォーカス**: Red-Green-Refactorの完全なサイクル
- **スコープ**: テスト + 実装 + リファクタリング
- **ゴール**: 高品質なプロダクションコードの完成

### 使い分け
- **テストファーストエージェント**: チームで役割分担する場合や、テスト設計に集中したい場合
- **TDDエージェント**: 一人で機能を完成させる場合や、継続的な品質改善が必要な場合

---

## 複数テストファイルの管理

### 原則
一つの実装に対して複数のテストファイルが存在することは正常であり、むしろ推奨されます。

### 分割の指針
1. **テストの粒度で分割**
   - 単体テスト（Unit）
   - 統合テスト（Integration）
   - E2Eテスト（End-to-End）

2. **テストの目的で分割**
   - 機能テスト（Functional）
   - パフォーマンステスト（Performance）
   - セキュリティテスト（Security）
   - 回帰テスト（Regression）

3. **テスト対象で分割**
   - 正常系テスト（Happy Path）
   - 異常系テスト（Error Cases）
   - 境界値テスト（Edge Cases）

### 命名規則
```
<対象>.<種類>.test.<拡張子>

例：
user.test.ts              # 基本的な単体テスト
user.validation.test.ts   # バリデーション特化
user.integration.test.ts  # 統合テスト
user.e2e.test.ts         # E2Eテスト
user.performance.test.ts  # パフォーマンステスト
```

### 実行戦略
- **開発時**: 単体テストを頻繁に実行
- **コミット前**: すべてのテストを実行
- **CI/CD**: 段階的にテストを実行（Unit → Integration → E2E）

---

## ベストプラクティス

### テストコード
- DRY原則（Don't Repeat Yourself）を適用
- AAAパターン（Arrange-Act-Assert）を使用
- テストごとに一つの概念をテスト
- テストの可読性を最優先
- フィクスチャやヘルパー関数を適切に使用

### 実装コード
- SOLID原則に従う
- テストしやすい設計を心がける
- 依存性注入を活用
- 副作用を最小化
- 単一責任の原則を守る

### ワークフロー
- 小さなステップで進める
- 頻繁にテストを実行する
- コミットメッセージで状態を明記（Red/Green/Refactor）
- 継続的インテグレーションを活用
- コードレビューでテストもレビュー対象とする

---

## まとめ

これらのエージェントは、品質の高いソフトウェアを効率的に開発するための強力なツールです。テストファーストエージェントでテストの設計を行い、TDDエージェントで完全な開発サイクルを実行することで、保守性が高く、バグの少ないコードベースを構築できます。

複数のテストファイルを適切に管理することで、テストの可読性と保守性が向上し、長期的なプロジェクトの成功に貢献します。
